import React, { PureComponent } from 'react';
import { Text } from "react-native";
import { styles } from '../../../assets/styles/comman-styles';
type Props = {
    note:string
}
export const GreyNote = ({note}:Props) => <Text style={{}}>{note}</Text> 